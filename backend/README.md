# Insurecast - Backend

Time-series models applied to insurance data

## Setup

```bash
# From project root
uv sync --dev
```

## Run

```bash
uv run uvicorn insurecast.main:app --reload
```

## Test

```bash
uv run pytest
```
